name: epson_epos_printer
description: A new flutter plugin project.
version: 0.0.1
homepage:

environment:
  sdk: '>=2.16.1 <3.0.0'
  flutter: '>=2.5.0'

dependencies:
  flutter:
    sdk: flutter

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^1.0.0

# For information on the generic Dart part of this file, see the
# following page: https://dart.dev/tools/pub/pubspec

# The following section is specific to Flutter.
flutter:
  plugin:
    platforms:
      android:
        package: me.timnew.epson_epos_printer
        pluginClass: EpsonEposPrinterPlugin
      ios:
        pluginClass: EpsonEposPrinterPlugin
